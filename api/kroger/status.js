import {COOKIE_NAME,configured,session,seal,cookie,apiGet,refreshAccess} from './_kroger.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(!configured())return res.status(200).json({configured:false,connected:false});let s=session(req);if(!s)return res.status(200).json({configured:true,connected:false});try{if(s.expires_at&&Date.now()>s.expires_at&&s.refresh_token){const t=await refreshAccess(s.refresh_token);s={...s,access_token:t.access_token,refresh_token:t.refresh_token||s.refresh_token,expires_at:Date.now()+((Number(t.expires_in)||1800)*1000)-30000,scope:t.scope||s.scope};res.setHeader('Set-Cookie',cookie(COOKIE_NAME,seal(s),60*60*24*30))}const profile=await apiGet('/identity/profile',s.access_token);return res.status(200).json({configured:true,connected:true,profileId:profile?.data?.id||profile?.id||null,scope:s.scope||''})}catch{return res.status(200).json({configured:true,connected:false,needsReconnect:true})}}
